# Public environment variables (safe to expose — Vite embeds these at build time)
# Copy this file to .env.local and fill in values

# Backend API base URL
# - Leave empty for development (Vite proxy will forward /api/* to localhost:5000)
# - Set to your deployed backend URL for production
VITE_API_URL=
